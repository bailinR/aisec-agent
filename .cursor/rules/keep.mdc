---
description: 本仓库全局约束与进度备忘（keep.md）
alwaysApply: true
---

# keep 约束

处理本仓库任何任务前，先阅读并遵守根目录 `keep.md`：

- 每次改完代码提交本地 Git（可不立刻 push）
- 双击 `start.bat` 只做本地启动，不对齐远程；需要更新远程用 `update-and-start.bat`
- 进度与已知坑以 `keep.md` 为准，有变化时同步更新该文件
