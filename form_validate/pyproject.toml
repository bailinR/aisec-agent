[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "form_validate"
version = "0.0.1.1"
description = "Local form validation decorator for aisec-agent"
requires-python = ">=3.10"
